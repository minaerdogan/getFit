import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF8CAEFF); // changed from green to bluish-purple
  static const Color secondary = Color(0xFFB1C8FF); // light bluish tone for gradient pairing
  static const Color textColor = Colors.black;
  static const Color classicButtonColor = Color(0xFF8CAEFF); // also updated to match the button
  static const Color lightColor = Color(0xFFB1C8FF);// softened light variant
}
